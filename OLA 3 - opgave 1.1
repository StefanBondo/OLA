com2 <- combn(tillid[1,3:ncol(tillid)],1)

combn(tillid[1,3:ncol(tillid)],2)
combn(tillid[2,3:ncol(tillid)],2)
combn(tillid[3,3:ncol(tillid)],2)
combn(tillid[4,3:ncol(tillid)],2)
combn(tillid[i,3:ncol(tillid)],2)

y <- colMeans(combn(as.numeric(tillid[3,3:ncol(tillid)]),2))

y
comliste <- list()
comliste2 <- list()
indikatorliste <- list()

for (i in seq_len(nrow(tillid_negativ))) {
  kvartal <- as.numeric(tillid_negativ[i, 3:ncol(tillid_negativ), drop = FALSE])
  com2 <- combn(kvartal, 1, simplify = FALSE)
  comliste[i] <- list(com2)
}

for (i in seq_len(nrow(tillid_negativ))) {
  x <- as.numeric(tillid_negativ[i, 3:ncol(tillid_negativ), drop = FALSE])
  combs_all <- unlist(
    lapply(1:length(x), function(k) combn(x, k, simplify = FALSE)))
  recursive = FALSE
  comliste2[i] <- list(combs_all)
}
